# 以图搜款

本上下文描述可独立检索的款式图片，以及图片与后续商品资料之间的关系。

## Language

**图片资产（Image Asset）**:
一张可独立参与相似图片搜索的款式图片。每个来源相对路径对应一项图片资产，即使不同路径下的文件内容完全相同；图片资产可以暂时没有商品型号。
_Avoid_: 产品图片、商品

**图片导入项（Image Import Item）**:
已经接收源图、但仍在等待或重试生成向量的导入工作项。它不是图片资产，不参与搜索或归款；向量成功后才转化为正式图片资产。
_Avoid_: 图片资产、未归款图片、失败图片

**未归款图片（Unassigned Image Asset）**:
尚未补充商品型号的图片资产。它仍然可以参与搜索，并以来源相对路径作为用户可识别信息。
_Avoid_: 孤儿图片、无效图片

**已归档图片（Archived Image Asset）**:
已移入回收站、但仍可恢复的图片资产。它不出现在以图向量搜索、名称或来源路径搜索及默认资产列表中，不会自动过期，只能由用户主动永久清除；归档不等于永久清除源图。
_Avoid_: 已删除图片、无效图片

**永久清除（Permanent Purge）**:
用户在回收站中明确确认后，对图片资产执行的不可逆日常操作。完成后应用不再承诺恢复；基础设施备份只用于管理员执行的灾难恢复。
_Avoid_: 归档、移入回收站、一键恢复

**永久清除安全门（Permanent Purge Safety Gate）**:
服务端对五项备份与恢复条件的合取判定。任一项未知、过期或失败则永久清除不可用。它不是前端按钮、不是环境开关。
_Avoid_: 环境开关、前端隐藏、回收站恢复

**安全门证据（Purge Gate Evidence）**:
ops 写入的、带过期时间的条件证明文档。应用只读文档，不在请求内执行备份或演练。能写证据目录的主体即能打开安全门，这是主机文件系统级信任边界，不属于本控制面的抗沦陷能力。
_Avoid_: 备份清单、环境开关、现场演练本身

**管理员令牌（Purge Admin Token）**:
仅用于永久清除控制面的共享秘密。它不是用户账号，也不保护搜索、导入、归款或回收站恢复。
_Avoid_: 登录会话、通用管理员账号

**图片资产操作记录（Image Asset Activity Record）**:
对图片改名、归档、恢复、导入处理和永久清除等领域动作的持久化追溯记录，保留一年。它不代替图片资产当前状态、运行日志或备份，也不得保存凭证、签名 URL 或图片内容。
_Avoid_: 应用日志、数据库备份、图片资产

**商品型号（Product Model）**:
后续人工补充到图片资产上的业务型号。商品型号不是图片资产入库或参与搜索的前提；多个独立图片资产可以关联同一个商品型号，关联不会合并这些资产。
_Avoid_: 图片 ID、文件夹名

**待完善商品（Draft Product）**:
已经有商品型号、但分类、链接、规格或价格等资料尚未补齐的商品。待完善状态不影响其关联图片参与搜索。
_Avoid_: 无效商品、临时商品

**源图（Source Image）**:
导入或上传时收到的原始图片文件。源图保留原始质量，不被搜索处理产生的压缩版本覆盖。
_Avoid_: 搜索图、缩略图

**搜索预览图（Search Preview）**:
由源图派生、适合视觉检索和界面展示的标准化图片。它可以重新生成，不取代源图。
_Avoid_: 原图、临时图

**查询图（Query Image）**:
用户为一次相似图片搜索临时提交的图片。查询图不属于图片资产库，搜索完成后不再保留。
_Avoid_: 图片资产、源图

**图片匹配结果（Image Match）**:
查询图与某一个图片资产之间的相似度结果。每个图片资产独立占据一个结果位置，即使多个结果关联同一商品型号也不折叠。
_Avoid_: 商品匹配结果、型号匹配结果

**来源相对路径（Source Relative Path）**:
用户用于定位源图的原始相对路径。它保持不可编辑，并作为可搜索的来源追溯信息，不承担图片资产的主要显示名称。
_Avoid_: 本地绝对路径、临时访问地址

**资产显示名称（Asset Display Name）**:
用户可修改、可搜索且允许重名的图片资产主要名称，用于日常识别，不作为资产身份，也不改变图片资产的来源身份或正式存储位置。尚未改名时默认使用来源相对路径最后一段的原文件名，归款不改变名称；所有活动资产均可改名，已归档资产需先恢复，且改名只改变主文件名、扩展名保持不变。
_Avoid_: 来源相对路径、OSS 对象名称、源文件名

**来源冲突（Source Conflict）**:
同一来源相对路径再次出现、但源图内容与已导入版本不同的状态。来源冲突必须经过明确选择后才能替换现有图片资产或形成新资产。
_Avoid_: 重复图片、自动更新

**图片导入项（Image Import Item）**:
一张已经通过图片校验并写入私有对象存储、但尚未保证成为正式图片资产的持久工作项。它只有 queued、embedding、completed、failed 四种状态；只有 embedding 模型、维度和数值全部合规且数据库事务提交成功后，才会关联正式图片资产。
_Avoid_: 图片资产、内存队列消息、上传线程

**任务领取租约（Import Claim Lease）**:
独立 worker 在 PostgreSQL 中取得的限时处理所有权，由 claim token、generation、worker 与过期时间共同界定。租约仅用于进程崩溃后的重新领取和多实例并发隔离，不代表业务重试策略。
_Avoid_: 自动重试、浏览器锁、请求内线程

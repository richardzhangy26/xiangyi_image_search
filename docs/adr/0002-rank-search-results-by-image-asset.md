---
status: accepted
---

# 搜索结果按图片资产独立排序

以图搜款返回图片级 Top-K，每个图片资产独立占据一个结果位置，不再按商品型号折叠。当前首要目标是定位具体相似图片及其来源相对路径；即使多个资产后来关联同一型号，它们仍可能分别出现在结果中。旧检索中的 `DISTINCT ON (model_number)` 与这一语义冲突，应从图片级检索路径移除；未来如需减少重复，应作为可选的展示模式另行设计。

---
status: accepted
---

# 图片资产独立于商品资料

图片资产可以在没有商品型号和完整商品资料时独立入库、生成向量并参与搜索，因此将其建模为独立的 `image_assets`，而不是继续作为 `products` 的必有子记录。商品资料以后通过可选关联补充；删除或修改商品资料不应连带删除源图、搜索预览图或既有向量。虽然让现有 `product_images.model_number` 可空的改动更小，但会保留错误的所有权语义，并让商品级联删除影响独立资产。
